

const state = {
  token:'123',
  name:'',
  avatar:'',
  roles:''
};

const mutations = {};

const actions = {};

const getters = {
  token:(state:any) => state.token,
};

export default {
  namespace:true,
  state,
  mutations,
  actions,
  getters
}
